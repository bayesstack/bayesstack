# BayesStack API Environment Settings

# Environment Mode
BAYESSTACK_ENV=development

# Multi-Tenant Resolution Base Domains
BASE_DOMAINS=localhost,bayesstack.com

# ----------------------------------------------------
# PostgreSQL Settings for Local Native Setup (Windows / Non-Docker)
# ----------------------------------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=bayesstack

# Or set complete connection string directly:
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/bayesstack

# ----------------------------------------------------
# PostgreSQL Settings for Docker Setup (Linux / Docker Desktop)
# ----------------------------------------------------
# When running with 'docker compose up', compose automatically sets:
# POSTGRES_HOST=postgres
# DATABASE_URL=postgresql+asyncpg://bayesstack:bayesstack_dev@postgres:5432/bayesstack
