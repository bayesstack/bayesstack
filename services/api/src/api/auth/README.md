# Auth module

Reserved for identity, tenant membership, and RBAC functionality.

Auth is intentionally an internal module of the single `api` server in version one. It does not have its own FastAPI application, port, or deployment boundary.
