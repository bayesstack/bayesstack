"use client";

import React, { useState } from "react";
import { Avatar, AvatarsGroup, Badge, Button, Chip, Drawer, Icon, Paper, SearchInput, Select, Tabs, Text, Textarea, Title } from "@bayesstack/ui";

export type DiscussionView = "hub" | "thread";

const discussions = [
  { title: "Gradient descent diverges when learning rate = 0.1", context: "Machine Learning / Gradient Descent", author: "You", initials: "SC", meta: "Prof. Rao replied", replies: 3, time: "18 min ago", attention: true, status: "Faculty responded", tone: "success" as const },
  { title: "Evaluation metric for imbalanced churn dataset", context: "Customer Churn Prediction / Project", author: "Aarav Jain", initials: "AJ", meta: "Sarah mentioned you", replies: 8, time: "1 hr ago", attention: true, status: "Mentioned", tone: "info" as const },
  { title: "Why does L2 regularisation shrink coefficients?", context: "Machine Learning / Regularisation", author: "Neha Kapoor", initials: "NK", meta: "Accepted answer", replies: 6, time: "24 min ago", attention: false, status: "Resolved", tone: "success" as const },
  { title: "JOIN returning duplicate rows", context: "Database Systems / Relational Algebra Lab", author: "Arjun Jain", initials: "AJ", meta: "Prof. Shah joined", replies: 4, time: "42 min ago", attention: false, status: "Faculty responded", tone: "success" as const },
  { title: "Should validation data be stratified?", context: "Machine Learning / Model Evaluation", author: "Rahul Mehta", initials: "RM", meta: "11 learners found this useful", replies: 11, time: "2 hr ago", attention: false, status: "Helpful answer", tone: "primary" as const },
];

export function DiscussionExperience({ view, onViewChange }: { view: DiscussionView; onViewChange: (view: DiscussionView) => void }) {
  return view === "thread" ? <DiscussionThread onBack={() => onViewChange("hub")} /> : <DiscussionsHub onOpenThread={() => onViewChange("thread")} />;
}

function DiscussionsHub({ onOpenThread }: { onOpenThread: () => void }) {
  const [tab, setTab] = useState("For you");
  const [askOpen, setAskOpen] = useState(false);
  const [search, setSearch] = useState("");
  return <div className="discussion-page discussion-page-v2">
    <header className="discussion-v2-header"><div><Text as="p" size="xs" color="tertiary" strong className="learner-panel-eyebrow">Academic conversations</Text><Title as="h1" weight="semibold">Get unstuck, together.</Title><Text as="p" size="md" color="secondary">Questions stay attached to the concept, lab step, project decision, or work that made them necessary.</Text></div><Button size="md" leftIcon="Plus" onClick={() => setAskOpen(true)}>Ask a question</Button></header>

    <section className="discussion-command-grid">
      <Paper className="discussion-help-card" radius="xl" elevation="lg" bordered={false} padding={0}><div className="discussion-help-mark"><Icon name="AiChat" size="xl" /></div><div><Badge color="info" variant="outline" prefixIcon="Sparkles">Context-aware help</Badge><Title as="h2" weight="semibold">Start with your work already attached.</Title><Text as="p" size="md">Bayes AI can diagnose the first issue privately. Escalate the same thread to classmates or faculty without rebuilding the context.</Text><div><Button size="sm" rightIcon="ArrowRight" onClick={() => setAskOpen(true)}>Ask from current context</Button><Button variant="link" size="sm">How academic help works</Button></div></div><aside><Text as="p" size="xs">Current context</Text><strong>Optimization Lab / Step 3</strong><span>Notebook cell 14 / learning_rate = 0.1</span><Chip color="success" variant="subtle" size="sm" prefixIcon="CheckCircle">Context ready</Chip></aside></Paper>
      <Paper className="discussion-community-pulse" radius="xl" elevation="md" padding={24}><div className="learner-panel-heading"><div><Text as="p" size="xs" color="tertiary" strong className="learner-panel-eyebrow">Course community</Text><Title as="h2" weight="semibold">Learning is active</Title></div><Badge color="success" variant="subtle" dot pulse /></div><div className="discussion-community-number"><strong>14</strong><span>classmates active in your courses</span></div><AvatarsGroup avatars={[{ name: "Neha Kapoor", color: "#fce7f3" }, { name: "Arjun Jain", color: "#e0f2fe" }, { name: "Rahul Mehta", color: "#dcfce7" }, { name: "Mira Shah", color: "#ede9fe" }]} total={14} limit={4} size="sm" /><div className="discussion-response-stat"><Icon name="Clock" size="sm" /><span><strong>18 min</strong><small>median helpful response</small></span></div></Paper>
    </section>

    <section className="discussion-explorer"><div className="discussion-explorer-top"><SearchInput value={search} onValueChange={setSearch} onClear={() => setSearch("")} size="lg" placeholder="Search questions, concepts, labs, or projects" aria-label="Search discussions" /><Button variant="outline" size="md" leftIcon="Sliders">Filters</Button></div><div className="discussion-explorer-nav"><Tabs value={tab} onValueChange={setTab} variant="pill" size="sm" items={[{ value: "For you", label: "For you", badge: <span>2</span> }, { value: "My discussions", label: "My discussions" }, { value: "Unanswered", label: "Unanswered", badge: <span>6</span> }, { value: "Resolved", label: "Resolved" }]} /><div><Select defaultValue="all" options={[{ value: 'all', label: 'All courses' }, { value: 'ml', label: 'Machine Learning' }, { value: 'db', label: 'Database Systems' }]} /><Select defaultValue="recent" options={[{ value: 'recent', label: 'Most recent' }, { value: 'helpful', label: 'Most helpful' }]} /></div></div></section>

    <section className="discussion-attention-v2"><div className="discussion-v2-section-heading"><div><Text as="p" size="xs" color="tertiary" strong className="learner-panel-eyebrow">Needs your attention</Text><Title as="h2" weight="semibold">Continue two conversations</Title></div><Badge color="danger" variant="subtle">2 updates</Badge></div><div>{discussions.filter((item) => item.attention).map((item) => <AttentionThread item={item} onOpen={onOpenThread} key={item.title} />)}</div></section>

    <section className="discussion-recent-v2"><div className="discussion-v2-section-heading"><div><Text as="p" size="xs" color="tertiary" strong className="learner-panel-eyebrow">Academic memory</Text><Title as="h2" weight="semibold">Useful conversations from your learning</Title></div><Button variant="link" size="sm" rightIcon="ArrowRight">Browse knowledge archive</Button></div><Paper className="discussion-table-v2" radius="xl" elevation="md" padding={0}>{discussions.filter((item) => !item.attention).map((item) => <DiscussionRow item={item} onOpen={onOpenThread} key={item.title} />)}</Paper></section>
    <AskDrawer open={askOpen} onClose={() => setAskOpen(false)} onPost={() => { setAskOpen(false); onOpenThread(); }} />
  </div>;
}

function AttentionThread({ item, onOpen }: { item: typeof discussions[number]; onOpen: () => void }) {
  return <Paper as="article" hoverable radius="xl" elevation="md" padding={22} className="discussion-attention-card"><div className="discussion-attention-top"><Avatar name={item.author} size="sm" /><span><strong>{item.author}</strong><small>{item.time}</small></span><Badge color={item.tone} variant="subtle" size="sm">{item.status}</Badge></div><Text as="p" size="xs" color="tertiary" strong>{item.context}</Text><Title as="h3" weight="semibold">{item.title}</Title><Text as="p" size="sm" color="secondary">{item.meta}. The full learning context and earlier responses are attached.</Text><footer><span><Icon name="Comment" size="sm" /> {item.replies} replies</span><Button variant="outline" size="sm" rightIcon="ArrowRight" onClick={onOpen}>Open conversation</Button></footer></Paper>;
}

function DiscussionRow({ item, onOpen }: { item: typeof discussions[number]; onOpen: () => void }) {
  return <button className="discussion-row-v2" onClick={onOpen}><Avatar name={item.author} size="sm" /><span><strong>{item.title}</strong><small>{item.context}</small></span><span><Badge color={item.tone} variant="subtle" size="sm">{item.status}</Badge><small>{item.author} / {item.time}</small></span><span><Icon name="Comment" size="xs" /> {item.replies}</span><Icon name="ChevronRight" size="xs" /></button>;
}

function DiscussionThread({ onBack }: { onBack: () => void }) {
  const [resolved, setResolved] = useState(false);
  const [escalated, setEscalated] = useState(false);
  const [reply, setReply] = useState("");
  return <div className="discussion-thread-page discussion-thread-v2"><Button variant="link" size="sm" leftIcon="ArrowLeft" onClick={onBack}>Back to discussions</Button><header className="thread-v2-header"><div><Text as="p" size="xs" color="tertiary" strong>Machine Learning / Gradient Descent / Notebook cell 14</Text><Title as="h1" weight="semibold">Why does my loss increase after iteration 20?</Title><div><Avatar name="Sarah Connor" size="xs" /><span>Asked by you / 22 min ago</span><Badge color={resolved ? 'success' : 'warning'} variant="subtle">{resolved ? 'Resolved' : 'Open'}</Badge></div></div><Button variant="outline" size="sm" leftIcon="Notification">Following</Button></header><div className="thread-v2-layout"><main className="thread-v2-conversation"><Paper as="article" radius="xl" elevation="md" padding={24} className="thread-v2-message"><div className="thread-v2-person"><Avatar name="Sarah Connor" size="sm" /><span><strong>Sarah Connor</strong><small>You / 22 min ago</small></span></div><Text as="p" size="md">I am implementing gradient descent and the loss starts increasing after iteration 20. I expected it to keep decreasing.</Text><pre>{`learning_rate = 0.1\nfor epoch in range(100):\n    w -= learning_rate * gradient`}</pre></Paper><Paper as="article" radius="xl" elevation="md" padding={24} className="thread-v2-message is-ai"><div className="thread-v2-person"><span className="thread-v2-ai"><Icon name="AiChat" size="sm" /></span><span><strong>Bayes AI</strong><small>Grounded in your course and execution context</small></span><Badge color="info" variant="subtle">First response</Badge></div><Text as="p" size="md">Your update is likely too large for this loss surface. Compare the curves at 0.01, 0.1, and 1.2, then inspect the values after iteration 18.</Text><div className="thread-v2-actions"><Text as="span" size="xs" color="tertiary">Did this get you unstuck?</Text><Button variant={resolved ? 'primary' : 'outline'} size="sm" leftIcon="CheckCircle" onClick={() => setResolved(true)}>Yes, resolve</Button><Button variant="outline" size="sm" leftIcon="UserGroup" onClick={() => setEscalated(true)}>Ask classmates</Button><Button variant="outline" size="sm" leftIcon="User" onClick={() => setEscalated(true)}>Ask faculty</Button></div>{escalated && <div className="thread-v2-escalated"><Icon name="CheckCircle" size="sm" /> Shared with the course discussion. The notebook context remains attached.</div>}</Paper><Paper as="article" radius="xl" elevation="md" padding={24} className="thread-v2-message"><div className="thread-v2-person"><Avatar name="Prof. N. Rao" size="sm" /><span><strong>Prof. N. Rao</strong><small>Faculty / 8 min ago</small></span><Badge color="primary" variant="subtle">Faculty</Badge></div><Text as="p" size="md">Also check the scale of your input features. A rate that works with normalized features can become unstable when one feature has much larger values.</Text><div className="thread-v2-actions"><Button variant="outline" size="sm" leftIcon="CheckCircle">Helpful</Button><Button variant="link" size="sm">Reply</Button></div></Paper><Paper className="thread-v2-reply" radius="xl" elevation="md" padding={18}><Textarea value={reply} onValueChange={setReply} rows={4} placeholder="Reply with a question, clarification, or useful answer..." /><footer><span><Icon name="ShieldCheck" size="xs" /> Context remains attached</span><Button size="sm" rightIcon="ArrowRight">Reply</Button></footer></Paper></main><ContextAnchor /></div></div>;
}

function ContextAnchor() {
  return <Paper as="aside" className="discussion-context-v2" radius="xl" elevation="md" padding={22}><div className="discussion-context-title"><span><Icon name="Layers" size="sm" /></span><div><Text as="p" size="xs" color="tertiary" strong className="learner-panel-eyebrow">Attached context</Text><Title as="h2" weight="semibold">Gradient Descent</Title></div></div><div className="context-path-v2"><span>ML 401</span><span>Chapter 2 / Optimization</span><strong>Notebook review / Cell 14</strong></div><div className="context-code-v2"><small>Current execution</small><pre>{`learning_rate = 0.1\nloss[20:] -> increasing`}</pre></div><div className="context-signal-v2"><Icon name="InfoCircle" size="sm" /><span><strong>Likely signal</strong><small>Step size or input scale</small></span></div><Button variant="outline" size="sm" fullWidth rightIcon="ArrowRight">Open in workspace</Button><div className="context-related-v2"><Text as="p" size="xs" color="tertiary" strong>Related concepts</Text><Chip size="sm" color="neutral" variant="outline">Loss functions</Chip><Chip size="sm" color="neutral" variant="outline">Feature scaling</Chip></div></Paper>;
}

function AskDrawer({ open, onClose, onPost }: { open: boolean; onClose: () => void; onPost: () => void }) {
  const [audience, setAudience] = useState("AI privately");
  const [question, setQuestion] = useState("Why does the validation loss increase while the training loss falls?");
  return <Drawer open={open} onClose={onClose} title="What are you stuck on?" subtitle="Start privately, then escalate with context" size="lg" className="ask-drawer-v2" footer={<><Text as="span" size="xs" color="tertiary"><Icon name="ShieldCheck" size="xs" /> Shared only with your chosen audience</Text><Button rightIcon="ArrowRight" onClick={onPost}>Ask</Button></>}><Textarea value={question} onValueChange={setQuestion} rows={5} showCount maxLength={600} /><Paper className="ask-context-v2" variant="subtle" radius="lg" elevation="none" padding={16}><Text as="p" size="xs" color="tertiary" strong>Context attached</Text><strong>Machine Learning / Gradient Descent</strong><small>Optimization Lab / Notebook cell 14 / execution trace</small><Badge color="success" variant="subtle" prefixIcon="CheckCircle">Ready</Badge></Paper><section className="ask-audience-v2"><Text as="p" size="xs" color="tertiary" strong>Who should help first?</Text>{[["AI privately","Only you and the grounded learning copilot","AiChat"],["Course discussion","Classmates and course faculty","UserGroup"],["Teaching staff","Escalate privately to faculty","User"]].map(([label,detail,icon]) => <button className={audience === label ? 'is-active' : ''} key={label} onClick={() => setAudience(label)}><span><Icon name={icon as any} size="sm" /></span><span><strong>{label}</strong><small>{detail}</small></span>{audience === label && <Icon name="CheckCircle" size="sm" />}</button>)}</section></Drawer>;
}
